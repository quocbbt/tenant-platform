server:
  port: 8082

spring:
  application:
    name: logiflow-service

  datasource:
    url: jdbc:postgresql://localhost:5432/tenant_platform_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true